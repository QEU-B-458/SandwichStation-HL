# Body prototype for the AI Deployment Borg — one hand only.
# Same pattern as Quadborg but restricted to a single hand.
- type: body
  id: AiDeploymentBorg
  name: "AI Deployment Borg"
  root: torso
  slots:
    torso:
      part: TorsoAnimal
      connections:
      - hands
    hands:
      part: HandsAnimal
